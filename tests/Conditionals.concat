/*

    INFO
    Testing Conditionals

    == Equal
    != Not Equal
    > Greater Than
    < Less Than
    >= Greater Than or equal to
    <= Less than or equal to
*/

macro space ' ' print endmacro
macro a 21 endmacro
macro b 20 endmacro

a b == print space
a b != print space
a b >  print space
a b <  print space
a b >= print space
a b <= print space
cr

// if a b >= then
//     69 print cr
// endif

// if a b != then
//     420 print cr
// endif

if 10 20 == then
    68 . 
elseif 10 20 == then
    print cr
endif

20 while dup 25 < do
    if dup 20 == then
        65 . cr
    elseif dup 21 == then
        66 . cr
    elseif dup 22 == then
        67 . cr
    else 
        dup print cr
    endif

    1 +
endwhile
